# hello.py
test flow 
A random file is a computer file structured to allow direct, non-sequential access to any record within it, often used for fast data retrieval. It enables reading or writing data at specific locations using functions like fseek or seekp. Dummy or dummy test files with random bytes can be generated to fill storage space, such as through the
